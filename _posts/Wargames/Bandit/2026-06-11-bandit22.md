---
title: "Bandit22 Writeup"
author: ekansh
date: 2026-06-11 18:31:24 +0530
categories: [Wargames, Bandit]
tags: [overthewire, bandit]
published : true
---

> Pass : tRae0UfB9v0UzbCdn9cY0gQnds9GF58Q
{: .prompt-info }

## Solution :

This is basically a follow-up to Bandit21, except this time the cron script doesn't just dump the password to a fixed filename — it computes the destination filename dynamically based on the username.

### Bash Transcript :

```bash
bandit22@bandit:~$ cd /etc/cron.d/
bandit22@bandit:/etc/cron.d$ cat cronjob_bandit23
@reboot bandit23 /usr/bin/cronjob_bandit23.sh  &> /dev/null
* * * * * bandit23 /usr/bin/cronjob_bandit23.sh  &> /dev/null
bandit22@bandit:/etc/cron.d$ cat /usr/bin/cronjob_bandit23.sh
#!/bin/bash

myname=$(whoami)
mytarget=$(echo I am user $myname | md5sum | cut -d ' ' -f 1)

echo "Copying passwordfile /etc/bandit_pass/$myname to /tmp/$mytarget"

cat /etc/bandit_pass/$myname > /tmp/$mytarget
bandit22@bandit:/etc/cron.d$ mytarget=$(echo I am user bandit23 | md5sum | cut -d ' ' -f 1)
bandit22@bandit:/etc/cron.d$ cat /tmp/$mytarget
0Zf11ioIjMVN551jX3CmStKLYqjk54Ga
bandit22@bandit:/etc/cron.d$
```

> The cron job runs as bandit23, so `$myname` evaluates to `bandit23` when the script runs. It then takes the string `"I am user bandit23"`, runs it through `md5sum`, and uses that hash as the filename to dump the password to in `/tmp`.

> Since this whole process is deterministic (same input string → same MD5 hash every time), we can just compute that same hash ourselves manually — substituting `bandit23` for `$myname` — and read the resulting file from `/tmp`, the same way the cron job's output would land there.