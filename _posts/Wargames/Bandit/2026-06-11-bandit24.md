---
title: "Bandit24 Writeup"
author: ekansh
date: 2026-06-11 18:31:24 +0530
categories: [Wargames, Bandit]
tags: [overthewire, bandit]
published : true
---

>Pass : gb8KRRCsshuZXI0tUuR6ypOFjiZbf3G8
{: .prompt-info }

## Level Goal

A daemon is listening on port 30002 and will give you the password for bandit25 if given the password for bandit24 and a secret numeric 4-digit pincode. There is no way to retrieve the pincode except by going through all of the 10000 combinations, called brute-forcing.
You do not need to create new connections each time

### Previous Failed Attempts : 

1. We directly used the command to send a request as follows : "echo "0000" | nc localhost PORT". And got the failed response. So we, write a bash script which would loop this process.
   till we get the correct code returned.
   #Mistake?! : The connection wouldn't close itself unless explicitly told to. So no more params are actually passed onto.
2. So to fix this issue we used the following command which would use a time out instead and wait for it to break as follows : "echo "0000" | nc localhost PORT -w 1" 
   #Mistake?! : Well it would take almost 3 hours and the connection would timeout giving us no defined output. Since it would take 1 sec for each number. 
3. We use another way where we would close the connection immediately after a response is recieved as follows : "echo "0000" | nc localhost PORT -q 0"
   Using this solution we designed a bash script which would check for correct response and output that. 
   #Mistake?! : We realized we were giving the wrong input the whole time. We also had to include the password of bandit24 in the input as well.  

## Solution : 

1. Go to /tmp directory and write a test_24.sh file as follows : 

for pin in {0000..9999}; do
    echo "BANDIT24_PASS $pin"
done | nc localhost 30002 | grep -v "Wrong"

2. Save this and execute and we get the following : 

```bash
bandit24@bandit:/tmp$ nano test_24.sh
Unable to create directory /home/bandit24/.local/share/nano/: No such file or directory
It is required for saving/loading search history or cursor positions.

bandit24@bandit:/tmp$ chmod +x test_24.sh
bandit24@bandit:/tmp$ ./test_24.sh
I am the pincode checker for user bandit25. Please enter the password for user bandit24 and the secret pincode on a single line, separated by a space.
Correct!
The password of user bandit25 is iCi86ttT4KSNe1armKiwbQNmB3YJP3q4

bandit24@bandit:/tmp$
```